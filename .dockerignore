# Dependencies
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output
.next
out

# Environment files (never bake secrets into image)
.env
.env.*
!.env.example

# Docker files (not needed inside image)
Dockerfile
docker-compose.yml
docker-compose*.yml
.dockerignore

# Nginx and scripts (server config, not app code)
nginx/
scripts/
certbot/

# Editor and OS artifacts
.git
.gitignore
.gitattributes
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db

# Docs
*.md
!README.md

# Test files
__tests__
*.test.*
*.spec.*
coverage/
